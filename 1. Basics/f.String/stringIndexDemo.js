let str = "Rahul";
let ch = str[0];
console.log(ch);
let ch2 = str[1]; // this is the way to get a character present at a particular index in a string.
console.log(ch2);

var len = str.length; // length is the property associated with string which gives the length of the string.

for(let i = 0; i < str.length; i++){
    console.log(i, str[i]);
}