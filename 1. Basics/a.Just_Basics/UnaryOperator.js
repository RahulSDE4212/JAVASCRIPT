let x = 5;
console.log(++x);
console.log(x++);

let z = "22";
let y = +x;
// + operator converts the operand on the right side into a number, if it is not already.
// It doesn't change the original operand instead returns a converted value.
let t = -x; 
// - operator converts the operand on the right side into a number and also convert it into negative.


console.log(typeof y);
console.log(typeof t);


/* Different types of unary operator.
    1. ++  -> unary addition operator.
    2. --  -> unary substraction operator.
    3. + -> unary plus opertor.
    4. - -> unary minus operator.
    5. typeOf 
    6. ! -> logical not operator

*/