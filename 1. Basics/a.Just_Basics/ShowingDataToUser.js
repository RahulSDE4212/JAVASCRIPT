/*
    ****** Showing Data To Users *******************
    -> we show data to users using "console.log(input)" method.
    -> console.log(input):
        > this function takes some data as input and display them in the output/console.
        > If we have two different console.log() function, then they display output in two different lines.
        > we can also pass multiple values together to console.log() and all of them will be on the same line.
            > console.log(10, "rahul", 43.2);
*/

var ch = 10;
console.log(10);
console.log(ch);
console.log(10 + 10);
let a = 25;
console.log(a);

console.log(10, "Rahul", a);