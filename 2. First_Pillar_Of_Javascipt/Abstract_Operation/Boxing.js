/*
    When we do boxing of a primitive datatype then it gets converted into non-primitive.
    -> for example. (1) -> Here we have done boxing of a primitive datatype 1 which is a number and hence it get converted into 
                           a number [1] object.
*/

//console.log(1.toString());
console.log((1).toString());
console.log(typeof 1);