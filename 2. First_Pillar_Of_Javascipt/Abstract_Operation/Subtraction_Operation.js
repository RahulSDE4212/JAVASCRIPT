/*
    **************** SUBTRACTION OPERATION ******************
    > substraction operation convert both of its operand into number, if not already.
    > javascript do this conversion which is called coercion(type casting).
*/

var res = "12" - "8";
console.log(res);